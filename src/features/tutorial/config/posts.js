// Ordering registry for tutorial pages. Each slug must have a matching
// loader entry in ../components/post-loader.js. Titles/descriptions/dates
// live in each page's own frontmatter, not here, so there is one source of
// truth.
export const tutorialPostSlugs = ['bat-dau'];
