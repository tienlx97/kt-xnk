// Required root convention for @next/mdx. The implementation lives in shared
// so runtime-compiled company docs and statically imported MDX use one mapping.
export { useMDXComponents } from './shared/components/mdx-components.jsx';
