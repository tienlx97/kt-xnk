# Tasks

- [x] 1.1 Add the common formatted number TextInput, migrate money, ratio,
  quantity, and weight fields, verify representative inputs in-browser, and
  pass `./harness/verify.sh`. Gate: `harness/runs/20260905-152655-7866/`.
- [x] 1.2 Accept three through eight digits after the decimal point, verify the
  editing behavior in-browser, and pass `./harness/verify.sh`. Gate:
  `harness/runs/20260905-155139-7971/`.
