# Tasks: Fullscreen Primary CRUD Dialogs

## 1. Primary create/edit workspaces

- [x] 1.1 Convert Contract, Commission, and `/admin/users` create/edit dialogs to fullscreen — verify: browser checks cover create and edit modes, header/footer remain visible, screenshots captured, and `./harness/verify.sh` passes.
