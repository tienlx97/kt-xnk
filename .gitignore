node_modules/
dist/
build/
.next/
.env
.env.*
!.env.example
!.env.production.example

# `astryx theme build` output — generated from src/shared/components/theme.js,
# see `pnpm theme:build` (runs automatically before dev/build/verify)
src/shared/components/kt-xnk.js
src/shared/components/kt-xnk.d.ts
src/shared/components/theme.built.css

# Verification evidence (per-run logs/screenshots) — keep out of git
harness/runs/

# memsearch (ADR-0002): memory markdown IS tracked (review before commit!);
# local state and indexes are not — .index-state.json/.watch.pid are
# machine-specific (collection name is derived from the absolute project
# path) and must never travel with the repo across machines.
.memsearch/*
!.memsearch/memory/
*.milvus.db
milvus.db

# Local Astryx reference scaffolds; application code lives under src/.
template/
