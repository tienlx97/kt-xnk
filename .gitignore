node_modules/
dist/
build/
.next/
.env
.env.*
!.env.example

# `astryx theme build` output — generated from src/ui/theme.js, see
# `pnpm theme:build` (runs automatically before dev/build/verify)
src/ui/kt-xnk.js
src/ui/kt-xnk.d.ts
src/ui/theme.built.css

# Verification evidence (per-run logs/screenshots) — keep out of git
harness/runs/

# memsearch (ADR-0002): memory markdown IS tracked (review before commit!);
# local state and indexes are not.
# .memsearch/*
# !.memsearch/memory/
*.milvus.db
milvus.db
